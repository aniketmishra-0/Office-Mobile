export const metadata = {
  title: "Edit Form — Office Mobile",
  description: "Edit your form",
};

export default function EditLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
